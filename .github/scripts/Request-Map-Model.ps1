Function Request-Map-Model {
  Param(
    [Parameter(Mandatory = $true , Position = 0, Type = "string")] $ROBLOSECURITY,
    [Parameter(Mandatory = $true , Position = 1, Type = "string")] $APIKEY,
    [Parameter(Mandatory = $false, Position = 3, Type = "string")] $MAP,
    [Parameter(Mandatory = $false, Position = 4, Type = "string")] $Patch
  )
  process {
    # Validation Process
  }
}